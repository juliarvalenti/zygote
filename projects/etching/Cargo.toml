[package]
name = "zygote-etching"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Ink on paper: noise warped by a cellular displacement map, traced by edge detection, dithered and printed as two solid colours"
publish = false

[[bin]]
name = "zygote-etching"
path = "src/main.rs"

[dependencies]
zygote-render.workspace = true
