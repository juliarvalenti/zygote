Use American English spelling in code, comments, and docs (e.g. "color", "center", "behavior", not "colour", "centre", "behaviour").

Agents (including remote sandbox sessions): do not monitor pull requests. Never subscribe to PR activity, schedule check-ins or reminders to re-check CI, or re-poll a PR's status unless the user explicitly asks in that conversation. Open the PR, report its state once, and stop.
